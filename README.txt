README

In order to run the controller, in the terminal simply cd to the correct directory and run “python track.py”. The libraries that are included on the git repo are needed to run this program. In order for the program to work, a purple object and a computer camera is needed.

In oder to run the game, simply run “python game4.py” in a terminal in the correct directory. 